Features List: Highlights core performance metrics like zero-dependency UI rendering, window virtualization for thousands of rows, and server-side integration capabilities.Quick Start Implementation: A copy-pasteable example mapping static mock data arrays to standard component bindings with built-in search and pagination tags active.API Props Documentation: Detailed markdown tables outlining definitions, default fallback values, and types for both core component layout structures and column schemas.Headless Hook Concept (useDataTable): Provides a demonstration showing advanced developers how to strip down default styles and wire up native HTML table rows using standard state handlers.
